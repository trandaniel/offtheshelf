function setCurrents() {
  //console.log(sessionStorage);
  // document.getElementById('currentName').value = session.pid;
  // document.getElementById('currentEmail').value = sessionStorage.user;
  // document.getElementById('currentStreetNum').value = sessionStorage.streetnumber;
  // document.getElementById('currentStreet').value = sessionStorage.street;
  // document.getElementById('currentCountry').value = sessionStorage.country;
  // document.getElementById('currentCity').value = sessionStorage.city;
}
